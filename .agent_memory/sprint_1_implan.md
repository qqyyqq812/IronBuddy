# 全息战局指引：冲刺期分工大纲 (Sprint 1)

> **⚠️ 注意所有特派 Agent**：不再使用 `docs/parallel/xxx` 中的旧散落文件。这是你当前唯一的目标航线！板端 IP 已确认为：`10.105.145.68`。

## 🛡️ Agent 1：【前端战区与状态机核心部】
**主攻方向**：Web 终极赛博仪表盘拼装，硬件级声频解耦，增加哑铃弯举状态机。
* **TODO-1**: 解决硬件底层声卡和 ASR（飞书/Coze）冲突问题，接通 DeepSeek 主动与被动语音交流反馈接口。
* **TODO-2**: 在 `main_claw_loop` 中开辟 `DumbbellCurlFSM` (哑铃弯举)，并在网页支持双状态无缝切换。
* **TODO-3**: 全面更新 HTML/JS —— 实现 2.5D CSS 筋肉闪烁引擎；利用服务器回传的分数建立“圆环百分比 UI（动作相似度）”。

## 🛰️ Agent 2：【云边协同样板间】
**主攻方向**：打通 UDP/TCP 板云通信桥梁。
* **TODO-1**: 开发 `Board_UDP_Sender.py` 与 `Cloud_RTMPose_Receiver.py`，确保低延迟画面的上传及反向坐标回推。
* **TODO-2**: 将通信协议与已在运行的 `ai_sensory/` 合流，做成动态开关：当云端不可用时，无缝掉回板端局部的 YOLOv5 备胎。

## 🧠 Agent 3：【多模态数据集统筹机】
**主攻方向**：打通极简 PyTorch 相似度模型与数据获取网。
* **TODO-1**: 设计 `experimental_data_collector.py` 数据镰刀，在“画面完整+检测到运动”时进行特征与视觉录制采集（包含三个分类标签：正常、代偿、不标准）。
* **TODO-2**: 撰写内置的“Mock EMG Engine（模拟肌电正弦发力发生器）”，以保证即使脱离物理传感器，也能生成测试态的特征向量矩阵。
* **TODO-3**: 在本地用几百条数据直接跑通一个微型非线性 MLP 或 GRU 网络，使其输出不再是 0/1 而是连贯浮点数（相似概率）。
