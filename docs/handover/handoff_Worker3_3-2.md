# 窗口转移交接卷宗：Worker_2 → Worker_3

> **生成时间**: 2026-03-30 11:15  
> **基准路径**: `/home/qq/projects/embedded-fullstack`

## 【项目当前状态】
1. **全局进度**：项目正处于 `Phase 3 纯软开发局 (Mode A 阶层主导制)`。
2. **Worker 2 (前端表现统帅)** 的下发节点 `2-1 (通信网前端并线)`、`2-2 (视觉呼吸大整容)`、`2-3 (绝缘报警机制层)` 已全部在纯软离线压测下打通。
3. **Worker 3 (后勤与宣发总长)** 的前置清理节点 `3-1 (历史阵列大清洗与防坑排雷图鉴)` 也已执行完毕（僵尸文件已被收拢至 `archive/V1_V2_legacy/`，防坑报告输出在了 `docs/parallel/IronBuddy_Deployment_Guide.md`）。
4. **当前停驻阻塞点**：大盘任务进度挂靠在 `docs/parallel/parallel_task.md` 的 **节点 3-2** 门前（目标是利用 Marp 引擎建立 `docs/presentations/` 下的高级文档即演示汇报环境）。

## 【已解决的核心问题】
- **双路高频并发渲染架构**：将 `streamer_app` 慢速 FSM (受 NPU 丢帧影响) 与 100Hz 的 UI 肌电管线 (/api/muscle_activation) 彻底剥离开，确保前端红条呼吸动画顺滑。
- **150ms 优雅传感器断连降级**：当底层进程发卡/停摆超过 150 毫秒后，采用绝对不遮挡原生摄像头监控和 3D 重建小人的 `sensor-alert-toast` (右上角轻量吐司)。
- **NPU 纯软打桩替身验证**：在不启动摄像头不连外接套件的情况下，依靠纯 Python 注入 `/dev/shm` 完成了 3D 骨架深蹲验证 (`index.html` 层级通关)。

## 【原生指令与红线约束】
继任的 Worker_3 必须严格遵循前任窗口用户定下的所有红线与偏好。以下是前任窗口内触发的原生 Prompt 一字不漏提取：
- **开发法则**：*“采用死磕的 Mode A（阶层主导制）！...每次你只能攻破 1 个节点。现在，请立刻带我进入【节点 x-x】的实机部署战斗...等我验收反馈后再做后续节点。”*
- **分工边界**：*“你去看一下你和worker1的任务分工，以及他的现状，你俩别重复，你的重点是展示本身”*
- **工作环境红线**：*“现在的问题是我摄像头暂时不在身边，小音箱也不允许响（在上课），所以你就展示页面就好。”*
- **UI 降级哲学**：*“你这个太夸张了，断连应该只是和传感器断开，其他模块正常工作，应该只是一个小报错，其他正常展示。”*
- **肌电通道过滤**：*“我的贴片最后应该只会贴在腿和胳膊上（股四头肌，腘绳肌，小腿肌，肱二头肌）。”*
- **核心卖点捍卫**：*“而且我们之前说过的那个视觉模块，关键是我们的3D小人会根据NPU的数据重建还原并低延迟跟我们一起动啊。”*
- **任务推进指引**：*“进行总结，然后给出接下来的implan / 不用你自己干，直接交给worker3”*

*(请仔细研读上述 Prompt，后续答辩材料必须突出：解耦的韧性、3D NPU 的低延迟与防鬼影重构机制，以及在硬件掉线时的坚韧展示能力。)*

## 【继任 Agent 介入节点】
请下一任 Agent 开局后，绝对不要进行自主扩散搜索，直接发起如下突击：
1. 阅读 `docs/parallel/parallel_task.md` 确认大盘局势（焦点为 3-2 和 3-3）。
2. 执行**节点 3-2**：读取 `/home/qq/.agents/workflows/add-presentation.md` 的规范。前往创建 `docs/presentations/` 环境并产出涵盖前述进展大纲的幻灯片占位骨架，随后弹出 `implan` 供老板审核！
