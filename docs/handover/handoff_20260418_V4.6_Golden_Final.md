# IronBuddy 窗口交接文档 (Vibe Coding Transfer Protocol)
> 创建时间: 2026-04-18 23:35 | 里程碑: V4.6 Golden Final

这是本注意窗口结束前自动提取的核心交接文档。继任 AI (Actor / Director) 请务必通读此文件，以了解上一轮会话解决的深坑和工程底线！不遵守此文档导致的系统崩溃将视为严重失职。

## 1. 用户原生 Prompt (一字不漏提取)
- **需求发起**："我现在希望把所有问题全部解决，然后进行代码的黄金项目沉淀，即把最新的代码沉淀到docs/GitHub_Golden_Pool/embedded-fullstack... 最终在对话框中需要完成最重要的任务，就是UI界面的最终确定以及各功能的最终确定..."
- **功能定调**："我希望的是有一个数据后台，持续更新... 或者直接语音，和飞书平台直连，但是推送的内容肯定要调用 API... 这样openclaw和deepseek的API也能共享数据，比如我问他我之前的历史数据他知道，这才是最重要的智能的作用。"
- **关于排布的严厉批评 (底线指令)**："你这个UI太拉了，换回原来的，之前那么多功能全没了，不要修改UI了，只需要修改这些功能。换回原来的。并给我讲解一下，为啥UI一换之前那些，肌电，啥的全都没了，我只是想换个排布好难啊。"
- **短平快查询需求**："飞书成功了，但是我问这个问题它不回答，不能像我同学那样快速反馈"
- **交接指令**："帮我把最新的代码进行黄金沉淀，更新各种现状。保证我下次上板能继续成功，其他AI能在你的基础上继续写代码且不会把以后的黄金代码破坏。同时保证其他AI一下能看出来我们的进度和层级"

## 2. 系统核心架构与定级进度 (V4.6 Golden)
**目前项目处于高度可用的 V4.6 成熟黄金版，功能完备且跑通。**
- **结构层级1：本地短路层 (Edge/Fast)**：对于类似“我做了多少个”的问题，由 `voice_daemon.py` 中的查房短路路由接管。拦截语音后，零延迟读取 `/dev/shm/fsm_state.json` 给出反馈，决不走 LLM 等待队列。
- **结构层级2：智能认知层 (Cognitive Nexus)**：当用户问及“总结/战报/推飞书”时，中台 (`streamer_app.py`) 调用 `cognitive_nexus.py` 扫读 SQLite 数据库近期的训练起伏历史，组配完整的时序上下文后让大模型 (DeepSeek) 发往飞书机器人。
- **结构层级3：高防守 UI 层 (前端原生态)**：原有的 3D/骨骼/图表渲染全数为 HTML5 Canvas 与 CSS id (`rigThigh`等) 强物理绑定在 `templates/index.html` 里面的巨量 JavaScript 循环中。

## 3. 防破坏黄金原则与后续开发高压红线
继任 AI 务必遵守以下“不准动”条约：
1. **【禁止触碰原生巨型 UI】**：极度禁止以“现代化、组件化、清理代码”的名义重写或删减 `templates/index.html` （共计2700行）。用户需要它保留“所有肌电与发光骨架信息”。任何想将里面的模块删去换排版的行为，会导致前端渲染画布引擎崩溃，后果由改动者负全责。**如果后续增加新按钮，仅允许微创悬挂或插入，坚决不能重建！**
2. **【语音意图分流隔离】**：如果在后续要增加语音功能，需严格写在 `hardware_engine/voice_daemon.py` 的意图捕获前列。如果是高频简单的数值查询，需强制短路。严禁将所有未命中词条作为透传语句发给远端，这会导致长超时并严重破坏系统反馈的敏捷性。

## 4. 相关代码锚点
- 飞书战报枢纽: `streamer_app.py`  `/api/feishu/push`
- 认知上下文处理器: `hardware_engine/cognitive/cognitive_nexus.py`
- 语音智能分流: `hardware_engine/voice_daemon.py` L600-650
- 前端防触碰古堡: `templates/index.html`
