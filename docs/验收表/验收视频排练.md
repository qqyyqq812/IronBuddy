验收视频排演：
我点击启动后，它语音提示教练已上线，随时准备指导。然后我开始做动作。我会故意做标准的和不标准的。现在是视觉模式，它就会提示不标准，然后我展示语音交互：让他调整疲劳度上限，

然后进行语音控制的静音和取消。

然后展示API调用的正常闲聊，我会问他一个健身问题：比如现在这个时间段适合做深蹲吗，然后他能够告诉我现在的真实时间，并分析是否适合。

这个时候我已经把视觉和听觉上的简单展示完毕，我将切换云端的GPU，看到效果更好，更流畅。

然后进入语音硬编码的切换动作和切换模式，现在要新加一个模式切换，我会先进行模式切换（就是纯视觉和视觉+传感），在新的模式中演示两种动作的切换，这样的话，就不需要做视觉的哑铃弯曲部分了。即纯视觉模块，我只做一个深蹲的。然后语音模式切换后我会先做深蹲的（无需MVC)，再进行语音的动作切换，做需要MVC的哑铃弯举。我不会进行任何手点操作了，所有都是语音操控。所以去掉那些UI交互，我最终的UI交互仅在设置里面可以进行云端的和板子本地的NPU模式切换，其他参数配置、启动和关闭一律改成语音播报。比如疲劳值满了清空，然后触发DeepSeek的API，这个就是自动触发。并且要语音汇报。
这部分首先的深蹲，我会做标准，不标准，代偿三个动作，看它的分类是否符合。请注意我的UI始终不变，比如机电传感器，如果说是纯视觉，那它就没有信号，如果说切换了之后，它就有信号。但是那个界面始终在
我的UI界面的计数也应该统一改成标准不标准代偿这三类，把合格率去掉。也不用区分深蹲和弯举，视觉模式下只计数标准和不标准，动作幅度和无模型都保持原来的不变，纯视觉就只有第一个，切换模式后第二个也启动
哑铃弯举的语音播报：切换动作后，进行语音播报，说：准备好后请说开始MVC测试。然后硬编码触发后，开始校准测试，进行语音提醒，比如说开始，然后结束后提示：测试已结束训练正式开始。注意语音硬编码：标准，不标准和代偿，我任何一个询。
然后训练完后进行飞书端的推送，告诉他帮我推送飞书，在训练完前让他推送。他说好的，已推送到飞书。
然后，我会展示他推送的飞书文本内容，并且我会切换到后台，看他本次有没有将我跟教练的对话以及训练数据记录下来。
最后语音硬编码请关机，它会说再见，请明天准时来锻炼。然后一键关闭。
这就是我的完整展示。
