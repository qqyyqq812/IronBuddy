# IronBuddy 项目展示 presentations文件夹规则

## 1. 最终展示汇报_Marp.md

### 这是最重要的文件，也是我最终的汇报文件，请帮我补充完整。
具体章节要求：

接下来我要做的所有工作都是已完成该展示文件的目标导向的。现在我把这个文档的，目录大纲给你，我们要做的就是把已有的已完成的先填进去，把未完成的全部空出来，但是我们自己就知道要完成什么了。
### 第一部分是已完成的部分，
第一章节是向同学们介绍我的项目，这里你也需要关键的明确一下，就是，几个关键词，YOLOv5，npu ，人体动作的神经网络识别，结合open claw和deepseekAPI，实现居家智能健身教练。然后把我们所使用的硬件，和最终实现的效果预期写出来。
### 第二章节是我如何跟板子建立连接的。使用的方法是用网线直接跟电脑连接，需要把具体的配置流程写上去，关键就是打开网络共享的那一步，跟板子建立通信后，使用终端将板子连上手机的热点，再把电脑也连上，手机的热点，这样它们在同一网段下就可以方便的开发了。然后就是使用agent进阶提高工作效率，我的Presentation文件夹下有图片，里面就有成功使用agent完成工作的截图，需要放上。(已经放了)
### 第三章节是硬件连接部分。
分为2个子章节：分别是摄像头，蜂鸣器和音响
摄像头需要包括免去摄像头如何识别出来，并且配置好它的设备树。并且把我尝试过的显示方法给列出来，第一个方法是远程桌面，后来被pass掉了，第二个方法则是，现在所使用的python推流到主机把渲染的工作交给主机来，这样可以避免卡顿。后续我的所有测试都直接集汇集在一张网页上，十分方便展示
包括复用了他的声卡语音输出,这个需要你来补充

蜂鸣器，则是gpIO口的一些相关操作，也是一个重点，如何写它的驱动等（这一部分是未完成的，需要你空出来）（这里可以说一下我是怎么配置的，有相关文件，使用了projects/embedded-fullstack/RK3399proX/3399 gpio 参考及例程/Rockchip_Developer_Guide_IO_CN.pdf中的教程）但是最终被音响替代了，这个需要你来补充

第三部分，则是小音箱。这个需要你来补充

然后就是第四章节npu神经网络YOLOV5，需要列出我是怎么选择这个神经网络的。需要明确为什么不选v7v8，以及找到的直接已经训练好的数据集，可以直接来拿来用。(当然这只是第一版本，是为了打通流程的)

第五章节则是，主循环。即工作起来的状态机。


下一章节就是具体的如何配置open claw和deep seekAPI，以及如何语音交互，还有我们拓展的飞书等功能

再下一个章节，就是整体的系统总结，它就是需要把我们所有的给联系起来，向他们展示一下，我们这个是如何工作的，展示一整个流程

同时，需要指导各个模块的连接方式。哪些是板子上的，哪些是主机上的，哪些是云端的。

最后就是之后的升级V2版本的一些内容，暂时空出来。

#最重要的：marp文件和各文件的写作要求：
### 1.使用skills，写作相关的技能
### 2.用平实，自然，不晦涩的报告风格的文风，不要有废话。
### 3.marp文件需要使用相关的marp语法，调用相关的skills，注意排版格式。
### 4.图文并茂，使用projects/embedded-fullstack/docs/presentations/images中的图片，并且如果有相应的位置需要插入图片，空出来等待我来插入。
### 5.marp文件需要有目录，并且可以点击跳转。

## 2. User用户手册_项目控制与运行图鉴.md
### 这是我的操作手册，里面放置我的手动操作终端命令，但是现在我已经把测试包装成了两个tcl，文件中的大量手动操作命令已经过时了，请以tcl为准，帮我删除。（然后把这里的要求也删除）

## 3. draft工作目录 projects/embedded-fullstack/docs/presentations/drafts

### 这个目录配合最终展示汇报_Marp.md使用，文件夹是各个模块的草稿，记录每个模块的开发过程，可以尽可能详细，现在这里面缺少了很多内容，请帮我补充完整。
这部分非常重要，是所有模块的权威文档，一定要根据所有模块的具体代码来补充，不要凭空捏造，而且要高质量，详细。

