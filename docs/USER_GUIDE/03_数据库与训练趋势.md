# 03 数据库与训练趋势

本章说明如何使用数据库页查看训练记录和跨日趋势。它把现场动作、页面反馈和后台数据连起来，证明系统不只是前端展示。

!!! info "V7.41-G 变更"
    主页工作台侧栏的"训练趋势"小卡片已**移除**（图太小、信息密度低）。完整训练趋势统一在 `/database` 页查看，含日级聚合、rep 级回放、跨日折线图。

## 页面入口

数据库页面由 Flask 提供，默认地址：

```
http://<BOARD_IP>:5000/database
```

页面读取板端 SQLite (`data/ironbuddy.db`) 中的训练 session、rep 事件、每日汇总和 LLM 日志。

## 8 张表与用途

验收时优先查看和训练闭环直接相关的数据表，其他表作为补充说明。

| 表 | 字段重点 | 验收用途 |
|------|---------|---------|
| `training_sessions` | start_ts / end_ts / total_good / total_failed / total_comp / fatigue_peak | 一次训练总览 |
| `rep_events` | rep_index / angle / min_angle / rom / visual_result / final_result / classification_source | rep 级回放，判定来源溯源 |
| `daily_summary` | date / total_volume / total_fatigue / mean_rom | 跨日趋势数据源 |
| `llm_log` | role / content / trigger / latency | 规划 / 问答 / 总结链路证据 |
| `user_config` | key / value | 用户偏好、疲劳上限 |
| `model_registry` | model_name / version / loaded_at | 加载的 GRU/NCC 模型登记 |
| `fatigue_v3_snapshots` | rep / d_eff / progress_pct | v3 ODE 疲劳积分快照（专利相关）|
| `event_log` | ts / source / type / payload | 系统事件审计 |

## 趋势查看流程

```mermaid
flowchart LR
    A[打开 /database] --> B[选 training_sessions 表]
    B --> C[查看最近 5-10 session]
    C --> D[切到 daily_summary]
    D --> E[7 天 / 30 天折线]
    E --> F[对照主页 hudReps 校验]
```

1. 打开 `http://<BOARD_IP>:5000/database`
2. 选 `training_sessions` 表，查看最近 5–10 个 session
3. 切到 `daily_summary` 区域，看 7 天 / 30 天柱状或折线
4. 对照主页面右下角的累计 reps 数，验证数据落库一致
5. 想看具体 rep 判定，切 `rep_events`，按 `session_id` 过滤

## 数据落库时机

!!! warning "stop 才入库"
    `IRONBUDDY_DB_DEFER_WRITES=1` 默认开启 — 训练过程中所有 rep 暂存内存，**点 STOP 或会话超时后**才批量写库。验收时一定要先停止训练再看数据库，否则只能看到上一次 session。

落库触发条件：

- 用户在主页按 **STOP** 按钮
- session 空闲超过 5 分钟（FSM 自动 timeout）
- 主进程被信号优雅关闭

## 常用 API 端点

| 端点 | 用途 |
|------|------|
| `GET /api/db/exercises` | 列出所有 exercise 名 |
| `GET /api/db/query?table=rep_events&limit=50` | 查询任意表（前 50 条）|
| `GET /api/db/diag` | DB 健康检查（表结构 / WAL 状态）|
| `GET /api/data/overview` | 今日 + 7 天聚合 |
| `POST /api/db/checkpoint` | 手动 checkpoint WAL |

## 常见问题

如果数据库页面没有展示预期数据：

- [x] **没看到本轮训练？** 先确认按了 STOP，触发写入
- [x] **数字短暂回退？** WAL 文件还没合并，30 秒内会一致
- [x] **趋势空？** `daily_summary` 表每天凌晨聚合，当天数据需先有 session 才有
- [x] **表结构错误？** 查 `/api/db/diag`，看是否有 schema 升级未执行

## 截图位

| 截图位 | 画面 | 占位 |
| --- | --- | --- |
| 03-1 | 数据库首页 | ![](images/screenshot-placeholder.svg) |
| 03-2 | training_sessions 表 | ![](images/screenshot-placeholder.svg) |
| 03-3 | rep_events 表 | ![](images/screenshot-placeholder.svg) |
| 03-4 | daily_summary 折线 | ![](images/screenshot-placeholder.svg) |
| 03-5 | llm_log 链路 | ![](images/screenshot-placeholder.svg) |

## 下一步

数据库能对上训练结果后，继续阅读 [04 语音控制与飞书周报](04_语音控制与飞书周报.md)。
