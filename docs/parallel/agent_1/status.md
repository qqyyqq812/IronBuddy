# 进度白板：Agent 1 — 语音通讯链路与状态机部
> ⏱️ 汇报时间：2026-04-06 23:15

## ✅ 已完成的里程碑
- [x] 解除系统底层死锁 — 解除了 `amixer` 音量强制软阴跌（1%与-f 200设定），打通了边缘板的大尺寸发生器输出；产出在 `voice_daemon.py`
- [x] 重构体验与消除假死 — 注入前端 JS `syncWorker` 加载动画屏蔽双击；将 DeepSeek Prompt 完全动态化并封禁了耗时巨大的长思维链 `<think>`。
- [x] 智能视角盲区填补 — 初步加入了 `l_score` 与 `r_score` 算法判断，尝试规避单纯只检测左腿引发的判定瘫痪。

## 🔄 进行中
- [/] 状态机的运动物理学标定 — 卡点：当前即便重写了左右腿判定，但用户站立进入测试境后，前端虽然视觉流和神经网络骨架渲染完美，深蹲动作仍旧无法突破判定下限（或一直卡在 `180°` 及 `等待入镜`/`锁定目标`）。这可能源于 `angle < 140` 和 `score` 的坐标偏移有异质性未被察觉。

## ⏳ 待启动
- [ ] 彻底抛弃简单的三角函数检测，引入更立体的滤波器或是交由接下来的（Agent 3）融合判断网络模型接管物理动作评估。

## 📢 对外依赖
- 亟待：**Agent 3 (肌电与视觉融合网端)** 尽快就位，因为这种基于纯 CV 坐标算三角函数的脆弱做法，迟早要被彻底替换掉。
