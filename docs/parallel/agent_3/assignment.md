# 任务卷宗：Agent 3 — MMF 神经网络与裁判定阶部
> 你负责本项目的绝杀底牌：Late Fusion 视觉-肌电后期多模态判评网络 (基于 PyTorch 与 NPU 落地的 MLP / GRU 构建)。

## 你的行为铁律
1. **一次对话只做一个里程碑。** 开工前先出 implan，用户审批通过后才动手。
2. **完成里程碑后：** 执行 `/para-sync` 更新 status.md，在 drafts/ 下沉淀产出文件。
3. **对话结束前：** 无论是否完成，必须同步 status.md 并更新 handoff/ 交接记录。

## 本轮里程碑
- **节点 1**：时序对齐探针的设计收集。(验收标准：获取 `RTMPose处理时间(\Delta t_1)` 与 `UDP 肌电延迟(\Delta t_2)`，稳定录制包含 `[骨骼帧, 肌电RMS]` 的 CSV 离线评估表单)。
- **节点 2**：极地训练与网络构建。(验收标准：用 PyTorch 设计能输入包含关键点与特征参数的轻量级 MLP，训练达到高度的“标准 vs 代偿”区分率)。

## 关键技术上下文
> [!IMPORTANT]
> 你的核心战区在 `hardware_engine/sensor/` 与 `main_claw_loop.py`。
我们的终场胜利条件是不依赖直接修改庞大耗时的视觉基干网络。我们采用提取特征拼接后过一次微型全连接网络。你将在本地完成模型（使用提取出来的数字特征），并协助把这个推理嵌回到主状态机的更新评分权重（`SquatStateMachine.update()`）中，作为捕捉借力作弊（代偿）的绝对终裁。
