# IronBuddy 交接文档
## 阶段：节点 1 & 2 理论实施方案落地
**交接时间：** 2026-04-06

### 1. 本次工作核心决策
在此次对话中，Agent 3 与 Director（用户）共同定调了关于 IronBuddy 核心融合模型的第一性原理方案。
放弃了复杂且消耗用户的“人工逐帧打标”预设。确立了利用**不同名字的 CSV 包揽不同真实训练状态（如 `lazy_squat.csv` 与 `standard_squat.csv`）**，在读取期全自动赋权 Ground Truth 的精简极客范式。
此外，明确了 Late Fusion 将不再动 RK3399 的底层视觉网络，而是作为 Python 层的极简挂件运行。

### 2. 当前遇到的问题与已知坑点
本地用户终端环境缺乏 RKNN，运行 `rtmpose_publisher.py` 会发生必定失败的 Exit 124 报错并降级到虚拟流。这不影响我们接下来的本地 Python 数据和逻辑脚本测试，但提醒我们在接管 `main_claw_loop.py` 测试时要使用 Mock 的 `/dev/shm` 数据来保证管道贯通。

### 3. 下一步起飞节点 (Next Steps)
我们建立的 `Task.md` 中的代码改造仍未开始，下一次启动时：
直接顺手承接这些任务！
1. 修改 `hardware_engine/sensor/udp_emg_server.py`，暴露极高频的心跳 Float。
2. 潜入 `main_claw_loop.py` ，提取骨架角度并拼接肌电。
3. 撰写 `fusion_model.py` 提供模型骨架！
