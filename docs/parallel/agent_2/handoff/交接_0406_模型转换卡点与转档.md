# Agent 2 进度与决策交接窗 
**交接时间**: 2026-04-06 23:15
**当前断点**: 模型由 ONNX 向 RKNN `.rknn` 实体格式进行纯净态脱板压缩（卡在环境配置）。

## 核心战果与成果物
1. **视觉外挂中枢开发妥当**：
   - IPC 发包主流：`hardware_engine/ai_sensory/vision/rtmpose_publisher.py`
   - 一欧元滤波护甲：`hardware_engine/ai_sensory/vision/filters.py`
   - 此两项已在纯 WSL 沙盒做过模拟，不依赖硬件，一切逻辑稳定。

2. **原料下行**：
   - 在 `tools/rtmpose-m_simcc-aicrowd_256x192.onnx` 下放置了 OpenMMLab 官方去重提取后的 54M 原型库。

## 重要决策与死角踩坑
- **Docker 崩盘断点**：经过排查确认（已生成报告 `docs/research/rknn_toolkit_docker.md`），基于第三方 Docker（如勒布霍伊维护版的 `lebhoryi/rknn-toolkit`）彻底团灭成为死链，而原厂 Docker 又锁在恶心的百度网盘内。
- **改弦易辙的决意（下一任行动指导）**：绝对不要再碰 Docker 这条废掉的时间线！我们当前有一份在脑海里挂着的**全新的 implementation plan**等待用户 Approve。
  - **下一步必须直接使用 WSL 本地的 Conda** 强杀出一个 Python3.6 的空壳！在这个空壳内从 Rockchip 的 github 上下载 Wheel 硬刚编译！然后驱动写好的 `tools/convert_rtmpose.py`，最后推板。

## 下次唤醒的首要指令
在下次唤醒接棒时，不要复盘寒暄，直接检查右侧用户是否对新的 `Implementation Plan` 给出了 `/approve` 或者通过其他形式许可了。
如果有许可，直接：`conda create -n rknn_env_py36 python=3.6 -y` 并推进即可。
